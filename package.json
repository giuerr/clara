{
  "name": "@tabularum/clara",
  "version": "6.0.0",
  "description": "Clara — Operations Lead Agent",
  "main": "index.js",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node --max-old-space-size=460 server.js",
    "test": "node --test test/*.test.js",
    "check-env": "node -e \"const r=['ANTHROPIC_API_KEY','GOOGLE_CLIENT_ID','GOOGLE_CLIENT_SECRET','GOOGLE_REFRESH_TOKEN','OWNER_EMAILS','OWNER_EMAIL','OWNER_NAME','CLARA_EMAIL'];const m=r.filter(k=>!process.env[k]);if(m.length){console.error('Missing env vars:',m.join(', '));process.exit(1);}console.log('All required env vars present.');\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "cors": "^2.8.5",
    "docx": "^9.6.1",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "googleapis": "^171.4.0",
    "pdf-parse": "^1.1.4"
  }
}
